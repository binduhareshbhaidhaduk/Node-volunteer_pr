<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Volunteer</title>
    <link rel="stylesheet" href="./css/style.css">
</head>
<body>

<main>
    <h2>Edit Volunteer</h2>

    <form method="post" action="/updateVolunteer/<%= singleVolunteer.id %>">
        <input type="hidden" name="id" value="<%= singleVolunteer.id %>" />

        <label for="yourname">First Name</label>
        <input type="text" name="yourname" value="<%= singleVolunteer.yourname %>" required />

        <label for="lastname">Last Name</label>
        <input type="text" name="lastname" value="<%= singleVolunteer.lastname %>" required />

        <label for="email">Email</label>
        <input type="email" name="email" value="<%= singleVolunteer.email %>" required />

        <label for="age">Age</label>
        <input type="number" name="age" value="<%= singleVolunteer.age %>" required />

        <label for="gender">Gender</label>
        <select name="gender" required>
            <option value="Male" <%= singleVolunteer.gender === 'Male' ? 'selected' : '' %>>Male</option>
            <option value="Female" <%= singleVolunteer.gender === 'Female' ? 'selected' : '' %>>Female</option>
            <option value="Other" <%= singleVolunteer.gender === 'Other' ? 'selected' : '' %>>Other</option>
        </select>

        <label for="address">Address</label>
        <input type="text" name="address" value="<%= singleVolunteer.address %>" required />

        <label for="city">City</label>
        <input type="text" name="city" value="<%= singleVolunteer.city %>" required />

        <label for="postcode">Postcode</label>
        <input type="text" name="postcode" value="<%= singleVolunteer.postcode %>" required />

        <label for="country">Country</label>
        <input type="text" name="country" value="<%= singleVolunteer.country %>" required />

        <label for="phonenum">Phone Number</label>
        <input type="tel" name="phonenum" value="<%= singleVolunteer.phonenum %>" required />

        <label for="emergency">Emergency Contact</label>
        <input type="tel" name="emergency" value="<%= singleVolunteer.emergency %>" required />

        <label for="skill">Skill</label>
        <input type="text" name="skill" value="<%= singleVolunteer.skill %>" required />

        <label for="qualification">Qualification</label>
        <input type="text" name="qualification" value="<%= singleVolunteer.qualification %>" required />

        <label for="reason">Why do you want to join us?</label>
        <textarea name="reason" required><%= singleVolunteer.reason %></textarea>

        <button type="submit">Save</button>
    </form>
</main>

<style>
    body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        color: #333;
        margin: 0;
        padding: 0;
    }

    h2 {
        text-align: center;
        color: #333;
    }

    form {
        max-width: 600px;
        margin: 20px auto;
        padding: 20px;
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    label {
        display: block;
        margin-bottom: 8px;
        font-weight: bold;
    }

    input, select, textarea {
        width: 100%;
        padding: 10px;
        margin-bottom: 20px;
        border: 1px solid #ddd;
        border-radius: 4px;
    }

    button {
        width: 100%;
        padding: 12px;
        background-color: #333;
        color: #fff;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 16px;
    }

    button:hover {
        background-color: #555;
    }
</style>

</body>
</html>
